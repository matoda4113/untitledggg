import 'package:flutter/material.dart';

class Dragon extends StatelessWidget {
  const Dragon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
    );
  }
}

